// Copy this file as a starting template for local development.
// Do NOT commit real secrets — keep them out of Git. Use CI or a secure vault.

export const environment = {
  production: false,
  // Example placeholder values — replace locally
  API_KEY: 'REPLACE_ME',
  AUTH_DOMAIN: 'REPLACE_ME',
};
